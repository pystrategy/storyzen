#:kivy 1.4

<Root>:
    progress: bar
    orientation: 'vertical'
    padding: 50

    ProgressBar:
        id: bar
        value: 100
        max: 200

    Slider:
        id: slider
        max: 200
        value: 100
        step: 50
        on_value: bar.value = self.value
